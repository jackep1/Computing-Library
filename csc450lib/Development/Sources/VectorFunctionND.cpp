#include <VectorFunctionND.h>
#include <CSC450Exception.h>

using namespace csc450lib_calc;

/**
 * VectorFunctionND constructor
 * 
 * @param n the number of dimensions
*/
VectorFunctionND::VectorFunctionND(int n) : N(n) {}

/**
 * Method for finding the partial derivatives of a function.
*/
std::vector<std::vector<float>> VectorFunctionND::grad(const std::vector<float>& x) const
{
    std::vector<std::vector<float>> gradient;

    

    return gradient;
}